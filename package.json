{
  "name": "todoapp",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm i) || (cd client && npm start)",
    "server": "node .server/server.js",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
